// Code generated by scripts/migrate from extern/tstl/test/unit/printer/deadCodeAfterBreak.spec.ts. DO NOT EDIT.
package tstltest

import (
	"testing"

	"github.com/realcoldfry/tslua/internal/transpiler"
)

func TestEval_DeadCodeAfterBreak(t *testing.T) {
	t.Parallel()

	batchExpectCodegen(t, []codegenTestCase{
		{"for dead code after break [5.0]", "function", `let result = 0;
        for (let i = 0; i < 10; i++) { result = i; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"for..in dead code after break [5.0]", "function", `let result = "";
        for (let a in {"a": 5, "b": 8}) { result = a; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"for..of dead code after break [5.0]", "function", `let result = 0;
        for (let a of [1,2,4]) { result = a; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"while dead code after break [5.0]", "function", `let result = "done";
        while (true) { break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"switch dead code after break [5.0]", "function", `let result = "none";
        switch ("abc" as string) {
            case "def":
                result = "def";
                break;
                let abc = 4;
            case "abc":
                result = "abc";
                break;
                let def = 6;
        }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"do-while dead code after break [5.0]", "function", `let result = "done";
        do { break; const b = 8; } while (true);
        return result;`, nil, []string{`local b = 8`}, nil, nil},
	}, WithLuaTarget(transpiler.LuaTargetLua50))

	batchExpectCodegen(t, []codegenTestCase{
		{"for dead code after break [5.1]", "function", `let result = 0;
        for (let i = 0; i < 10; i++) { result = i; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"for..in dead code after break [5.1]", "function", `let result = "";
        for (let a in {"a": 5, "b": 8}) { result = a; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"for..of dead code after break [5.1]", "function", `let result = 0;
        for (let a of [1,2,4]) { result = a; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"while dead code after break [5.1]", "function", `let result = "done";
        while (true) { break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"switch dead code after break [5.1]", "function", `let result = "none";
        switch ("abc" as string) {
            case "def":
                result = "def";
                break;
                let abc = 4;
            case "abc":
                result = "abc";
                break;
                let def = 6;
        }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"do-while dead code after break [5.1]", "function", `let result = "done";
        do { break; const b = 8; } while (true);
        return result;`, nil, []string{`local b = 8`}, nil, nil},
	}, WithLuaTarget(transpiler.LuaTargetLua51))

	batchExpectCodegen(t, []codegenTestCase{
		{"for dead code after break [universal]", "function", `let result = 0;
        for (let i = 0; i < 10; i++) { result = i; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"for..in dead code after break [universal]", "function", `let result = "";
        for (let a in {"a": 5, "b": 8}) { result = a; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"for..of dead code after break [universal]", "function", `let result = 0;
        for (let a of [1,2,4]) { result = a; break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"while dead code after break [universal]", "function", `let result = "done";
        while (true) { break; const b = 8; }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"switch dead code after break [universal]", "function", `let result = "none";
        switch ("abc" as string) {
            case "def":
                result = "def";
                break;
                let abc = 4;
            case "abc":
                result = "abc";
                break;
                let def = 6;
        }
        return result;`, nil, []string{`local b = 8`}, nil, nil},
		{"do-while dead code after break [universal]", "function", `let result = "done";
        do { break; const b = 8; } while (true);
        return result;`, nil, []string{`local b = 8`}, nil, nil},
	}, WithLuaTarget(transpiler.LuaTargetUniversal))

	t.Run("for dead code after break [5.0]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = 0;
        for (let i = 0; i < 10; i++) { result = i; break; const b = 8; }
        return result;`, `0`, WithLuaTarget(transpiler.LuaTargetLua50))
	})

	t.Run("for dead code after break [5.1]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = 0;
        for (let i = 0; i < 10; i++) { result = i; break; const b = 8; }
        return result;`, `0`, WithLuaTarget(transpiler.LuaTargetLua51))
	})

	t.Run("for dead code after break [universal]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = 0;
        for (let i = 0; i < 10; i++) { result = i; break; const b = 8; }
        return result;`, `0`, WithLuaTarget(transpiler.LuaTargetUniversal))
	})

	t.Run("for..in dead code after break [5.0]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "";
        for (let a in {"a": 5, "b": 8}) { result = a; break; const b = 8; }
        return result;`, `"a"`, WithLuaTarget(transpiler.LuaTargetLua50))
	})

	t.Run("for..in dead code after break [5.1]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "";
        for (let a in {"a": 5, "b": 8}) { result = a; break; const b = 8; }
        return result;`, `"a"`, WithLuaTarget(transpiler.LuaTargetLua51))
	})

	t.Run("for..in dead code after break [universal]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "";
        for (let a in {"a": 5, "b": 8}) { result = a; break; const b = 8; }
        return result;`, `"a"`, WithLuaTarget(transpiler.LuaTargetUniversal))
	})

	t.Run("for..of dead code after break [5.0]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = 0;
        for (let a of [1,2,4]) { result = a; break; const b = 8; }
        return result;`, `1`, WithLuaTarget(transpiler.LuaTargetLua50))
	})

	t.Run("for..of dead code after break [5.1]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = 0;
        for (let a of [1,2,4]) { result = a; break; const b = 8; }
        return result;`, `1`, WithLuaTarget(transpiler.LuaTargetLua51))
	})

	t.Run("for..of dead code after break [universal]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = 0;
        for (let a of [1,2,4]) { result = a; break; const b = 8; }
        return result;`, `1`, WithLuaTarget(transpiler.LuaTargetUniversal))
	})

	t.Run("while dead code after break [5.0]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "done";
        while (true) { break; const b = 8; }
        return result;`, `"done"`, WithLuaTarget(transpiler.LuaTargetLua50))
	})

	t.Run("while dead code after break [5.1]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "done";
        while (true) { break; const b = 8; }
        return result;`, `"done"`, WithLuaTarget(transpiler.LuaTargetLua51))
	})

	t.Run("while dead code after break [universal]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "done";
        while (true) { break; const b = 8; }
        return result;`, `"done"`, WithLuaTarget(transpiler.LuaTargetUniversal))
	})

	t.Run("switch dead code after break [5.0]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "none";
        switch ("abc" as string) {
            case "def":
                result = "def";
                break;
                let abc = 4;
            case "abc":
                result = "abc";
                break;
                let def = 6;
        }
        return result;`, `"abc"`, WithLuaTarget(transpiler.LuaTargetLua50))
	})

	t.Run("switch dead code after break [5.1]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "none";
        switch ("abc" as string) {
            case "def":
                result = "def";
                break;
                let abc = 4;
            case "abc":
                result = "abc";
                break;
                let def = 6;
        }
        return result;`, `"abc"`, WithLuaTarget(transpiler.LuaTargetLua51))
	})

	t.Run("switch dead code after break [universal]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "none";
        switch ("abc" as string) {
            case "def":
                result = "def";
                break;
                let abc = 4;
            case "abc":
                result = "abc";
                break;
                let def = 6;
        }
        return result;`, `"abc"`, WithLuaTarget(transpiler.LuaTargetUniversal))
	})

	t.Run("do-while dead code after break [5.0]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "done";
        do { break; const b = 8; } while (true);
        return result;`, `"done"`, WithLuaTarget(transpiler.LuaTargetLua50))
	})

	t.Run("do-while dead code after break [5.1]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "done";
        do { break; const b = 8; } while (true);
        return result;`, `"done"`, WithLuaTarget(transpiler.LuaTargetLua51))
	})

	t.Run("do-while dead code after break [universal]", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let result = "done";
        do { break; const b = 8; } while (true);
        return result;`, `"done"`, WithLuaTarget(transpiler.LuaTargetUniversal))
	})
}
