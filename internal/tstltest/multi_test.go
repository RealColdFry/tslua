// Code generated by scripts/migrate from extern/tstl/test/unit/language-extensions/multi.spec.ts. DO NOT EDIT.
package tstltest

import "testing"

func TestEval_Multi(t *testing.T) {
	t.Parallel()

	batchExpectDiagnostics(t, []diagTestCase{
		{"invalid direct $multi function use (let a; [a] = $multi())", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a; [a] = $multi()
        export { a };`, []int32{100032}, nil},
		{"invalid direct $multi function use (const [a] = $multi())", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const [a] = $multi()
        export { a };`, []int32{100032}, []string{`local ____exports = {}
local function multi(self, ...)
    return ...
end
local a = ____(nil)
____exports.a = a
return ____exports`}},
		{"invalid direct $multi function use (const [a] = $multi(1))", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const [a] = $multi(1)
        export { a };`, []int32{100032}, []string{`local ____exports = {}
local function multi(self, ...)
    return ...
end
local a = ____(nil, 1)
____exports.a = a
return ____exports`}},
		{"invalid direct $multi function use (const [a = 1] = $multi())", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const [a = 1] = $multi()
        export { a };`, []int32{100032}, []string{`local ____exports = {}
local function multi(self, ...)
    return ...
end
local a = ____(nil)
if a == nil then
    a = 1
end
____exports.a = a
return ____exports`}},
		{"invalid direct $multi function use (const [a = 1] = $multi(2))", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const [a = 1] = $multi(2)
        export { a };`, []int32{100032}, []string{`local ____exports = {}
local function multi(self, ...)
    return ...
end
local a = ____(nil, 2)
if a == nil then
    a = 1
end
____exports.a = a
return ____exports`}},
		{"invalid direct $multi function use (const ar = [1]; const [a] = $multi(...ar))", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const ar = [1]; const [a] = $multi(...ar)
        export { a };`, []int32{100032}, []string{`local ____exports = {}
local function multi(self, ...)
    return ...
end
local ar = {1}
local a = ____(
    nil,
    table.unpack(ar)
)
____exports.a = a
return ____exports`}},
		{"invalid direct $multi function use (const _ = null, [a] = $multi(1))", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const _ = null, [a] = $multi(1)
        export { a };`, []int32{100032}, []string{`local ____exports = {}
local function multi(self, ...)
    return ...
end
local _ = nil
local a = ____(nil, 1)
____exports.a = a
return ____exports`}},
		{"invalid direct $multi function use (let a: any; for (const [a] = $multi(1, 2); false; 1) {})", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a: any; for (const [a] = $multi(1, 2); false; 1) {}
        export { a };`, []int32{100032}, nil},
		{"invalid direct $multi function use (let a: any; for ([a] = $multi(1, 2); false; 1) {})", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a: any; for ([a] = $multi(1, 2); false; 1) {}
        export { a };`, []int32{100032}, nil},
		{"invalid direct $multi function use (let a: any; if ([a] = $multi(1)) { ++a; })", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a: any; if ([a] = $multi(1)) { ++a; }
        export { a };`, []int32{100032}, []string{`local ____exports = {}
local function multi(self, ...)
    return ...
end
local a
local ____temp_0 = {____(nil, 1)}
a = ____temp_0[1]
____exports.a = a
if ____temp_0 then
    a = a + 1
    ____exports.a = a
end
____exports.a = a
return ____exports`}},
		{"invalid $multi call ($multi)", "module", `$multi`, []int32{100032}, []string{`local ____ = ____`}},
		{"invalid $multi call ($multi())", "module", `$multi()`, []int32{100032}, []string{`____(_G)`}},
		{"invalid $multi call (({ $multi });)", "module", `({ $multi });`, nil, nil},
		{"invalid $multi call (const a = $multi();)", "module", `const a = $multi();`, []int32{100032}, []string{`a = {____(_G)}`}},
		{"invalid $multi call (const {} = $multi();)", "module", `const {} = $multi();`, []int32{100032}, []string{`local ____temp_0 = {{____(_G)}}`}},
		{"invalid $multi call (([a] = $multi(1)) => {})", "module", `([a] = $multi(1)) => {}`, []int32{100032}, nil},
		{"invalid $multi call (const [a = 0] = $multi())", "module", `const [a = 0] = $multi()`, []int32{100032}, []string{`a = ____(_G)
if a == nil then
    a = 0
end`}},
		{"disallow LuaMultiReturn non-numeric access", "function", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        return multi()['forEach'];`, []int32{100034}, nil},
		{"disallow LuaMultiReturn non-numeric access", "function", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        return multi().forEach;`, []int32{100034}, nil},
		{"invalid $multi implicit cast", "module", `function badMulti(): [string, number] {
            return $multi("foo", 42);
        }`, []int32{100033}, []string{`function badMulti(self)
    return "foo", 42
end`}},
	}, WithLanguageExtensions())

	batchExpectCodegen(t, []codegenTestCase{
		{"invalid direct $multi function use (let a; [a] = $multi())", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a; [a] = $multi()
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (const [a] = $multi())", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const [a] = $multi()
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (const [a] = $multi(1))", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const [a] = $multi(1)
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (const [a = 1] = $multi())", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const [a = 1] = $multi()
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (const [a = 1] = $multi(2))", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const [a = 1] = $multi(2)
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (const ar = [1]; const [a] = $multi(...ar))", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const ar = [1]; const [a] = $multi(...ar)
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (const _ = null, [a] = $multi(1))", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const _ = null, [a] = $multi(1)
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (let a: any; for (const [a] = $multi(1, 2); false; 1) {})", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a: any; for (const [a] = $multi(1, 2); false; 1) {}
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (let a: any; for ([a] = $multi(1, 2); false; 1) {})", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a: any; for ([a] = $multi(1, 2); false; 1) {}
        export { a };`, nil, nil, nil, nil},
		{"invalid direct $multi function use (let a: any; if ([a] = $multi(1)) { ++a; })", "module", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a: any; if ([a] = $multi(1)) { ++a; }
        export { a };`, nil, nil, nil, nil},
		{"invalid $multi call ($multi)", "module", `$multi`, nil, nil, nil, nil},
		{"invalid $multi call ($multi())", "module", `$multi()`, nil, nil, nil, nil},
		{"invalid $multi call (({ $multi });)", "module", `({ $multi });`, nil, nil, nil, nil},
		{"invalid $multi call (const a = $multi();)", "module", `const a = $multi();`, nil, nil, nil, nil},
		{"invalid $multi call (const {} = $multi();)", "module", `const {} = $multi();`, nil, nil, nil, nil},
		{"invalid $multi call (([a] = $multi(1)) => {})", "module", `([a] = $multi(1)) => {}`, nil, nil, nil, nil},
		{"invalid $multi call (const [a = 0] = $multi())", "module", `const [a = 0] = $multi()`, nil, nil, nil, nil},
		{"disallow LuaMultiReturn non-numeric access", "function", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        return multi()['forEach'];`, nil, nil, nil, nil},
		{"disallow LuaMultiReturn non-numeric access", "function", `function multi(...args: unknown[]) {
    return $multi(...args);
}

        return multi().forEach;`, nil, nil, nil, nil},
		{"invalid $multi implicit cast", "module", `function badMulti(): [string, number] {
            return $multi("foo", 42);
        }`, nil, nil, nil, nil},
		{"LuaMultiReturn in LuaIterable (#1591)", "module", `type IterableAlias = LuaIterable<LuaMultiReturn<[number, number]>>;

        declare const iterable: IterableAlias;

        for (const [a, b] of iterable) {}`, []string{`a, b`}, nil, nil, nil},
		{"LuaMultiReturn in LuaIterable intersection (#1591)", "module", `declare function iterator(): { a: string } & LuaIterable<LuaMultiReturn<[string, string]>>;

        for (const [a, b] of iterator()) {}`, []string{`a, b`}, nil, nil, nil},
	}, WithLanguageExtensions())

	t.Run("multi example use case", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multiReturn(): LuaMultiReturn<[string, number]> {
            return $multi("foo", 5);
        }

        const [a, b] = multiReturn();
        export { a, b };`, `{a = "foo", b = 5}`, WithLanguageExtensions())
	})

	t.Run("Destructuring assignment of LuaMultiReturn", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multiReturn(): LuaMultiReturn<[number, number, number]> {
            return $multi(1, 2, 3);
        }

        const [a, ...b] = multiReturn();
        export {a, b};`, `{a = 1, b = {2, 3}}`, WithLanguageExtensions())
	})

	t.Run("Destructuring assignment of LuaMultiReturn returning nil", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multiReturn(): LuaMultiReturn<[number, number, number]> {
            return;
        }

        const [a, ...b] = multiReturn();
        export {a, b};`, `{b = {}}`, WithLanguageExtensions(), WithOptions(map[string]any{"strict": false}))
	})

	t.Run("$multi call on return statement ($multi())", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `return $multi();`, `nil`, WithLanguageExtensions())
	})

	t.Run("$multi call on return statement ($multi(true))", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `return $multi(true);`, `true`, WithLanguageExtensions())
	})

	t.Run("$multi call on return statement ($multi(1, 2))", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `return $multi(1, 2);`, `1`, WithLanguageExtensions())
	})

	t.Run("valid indirect $multi function use (let a; [a] = multi())", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            let a; [a] = multi()
            export { a };`, `nil`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (const [a] = multi())", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            const [a] = multi()
            export { a };`, `nil`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (const [a] = multi(1))", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            const [a] = multi(1)
            export { a };`, `1`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (const [a = 1] = multi())", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            const [a = 1] = multi()
            export { a };`, `1`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (const [a = 1] = multi(2))", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            const [a = 1] = multi(2)
            export { a };`, `2`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (const ar = [1]; const [a] = multi(...ar))", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            const ar = [1]; const [a] = multi(...ar)
            export { a };`, `1`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (const _ = null, [a] = multi(1))", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            const _ = null, [a] = multi(1)
            export { a };`, `1`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (let a: any; for (const [a] = multi(1, 2); false; 1) {})", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            let a: any; for (const [a] = multi(1, 2); false; 1) {}
            export { a };`, `nil`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (let a: any; for ([a] = multi(1, 2); false; 1) {})", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            let a: any; for ([a] = multi(1, 2); false; 1) {}
            export { a };`, `1`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("valid indirect $multi function use (let a: any; if ([a] = multi(1)) { ++a; })", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

            let a: any; if ([a] = multi(1)) { ++a; }
            export { a };`, `2`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("function to spread multi type result from multi type function", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

        function m() {
            return $multi(...multi(true));
        }
        return m();`, `true`, WithLanguageExtensions())
	})

	t.Run("$multi call with destructuring assignment side effects", func(t *testing.T) {
		t.Parallel()
		expectModule(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

        let a;
        export { a };
        [a] = multi(1);`, `1`, WithLanguageExtensions(), WithReturnExport("a"))
	})

	t.Run("allow $multi call in ArrowFunction body", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `const call = () => $multi(1);
        const [result] = call();
        return result;`, `1`, WithLanguageExtensions())
	})

	t.Run("forward $multi call", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function foo() { return $multi(1, 2); }
        function call() { return foo(); }
        const [resultA, resultB] = call();
        return [resultA, resultB];`, `{1, 2}`, WithLanguageExtensions())
	})

	t.Run("forward $multi call indirect", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function foo() { return $multi(1, 2); }
        function call() { const m = foo(); return m; }
        const [resultA, resultB] = call();
        return [resultA, resultB];`, `{1, 2}`, WithLanguageExtensions())
	})

	t.Run("forward $multi call in ArrowFunction body", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `const foo = () => $multi(1, 2);
        const call = () => foo();
        const [resultA, resultB] = call();
        return [resultA, resultB];`, `{1, 2}`, WithLanguageExtensions())
	})

	t.Run("$multi call in function typed as any", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function foo(): any { return $multi(1, 2); }
        return foo()`, `1`, WithLanguageExtensions())
	})

	t.Run("$multi call cast to any", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function foo() { return $multi(1, 2) as any; }
        return foo()`, `1`, WithLanguageExtensions())
	})

	t.Run("$multi call cast to MultiReturn type", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function foo() { return $multi(1, 2) as unknown as LuaMultiReturn<number[]>; }
        return foo()`, `1`, WithLanguageExtensions())
	})

	t.Run("allow LuaMultiReturn numeric access (0)", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const i = 0;
        return multi(1)[0];`, `1`, WithLanguageExtensions())
	})

	t.Run("allow LuaMultiReturn numeric access (i)", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

        const i = 0;
        return multi(1)[i];`, `1`, WithLanguageExtensions())
	})

	t.Run("overloaded $multi/non-$multi return", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multiOverload(flag: true): LuaMultiReturn<[string, number]>;
        function multiOverload(flag: false): [string, number];
        function multiOverload(flag: boolean) {
            if (flag) {
                return $multi("foo", 4);
            } else {
                return ["bar", 7];
            }
        }
        const [x, y] = multiOverload(true);
        return y;`, `4`, WithLanguageExtensions())
	})

	t.Run("overloaded $multi/non-$multi return", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multiOverload(flag: true): LuaMultiReturn<[string, number]>;
        function multiOverload(flag: false): [string, number];
        function multiOverload(flag: boolean) {
            if (flag) {
                return $multi("foo", 4);
            } else {
                return ["bar", 7];
            }
        }
        const [x, y] = multiOverload(false);
        return y;`, `7`, WithLanguageExtensions())
	})

	t.Run("return $multi from try", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multiTest() {
            try {
                return $multi(1, 2);
            } catch {
            }
        }
        const [_, a] = multiTest()!;
        return a;`, `2`, WithLanguageExtensions())
	})

	t.Run("return $multi from catch", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multiTest() {
            try {
                throw "error";
            } catch {
                return $multi(1, 2);
            }
        }
        const [_, a] = multiTest();
        return a;`, `2`, WithLanguageExtensions())
	})

	t.Run("return LuaMultiReturn from try", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

        function multiTest() {
            try {
                return multi(1, 2);
            } catch {
            }
        }
        const [_, a] = multiTest()!;
        return a;`, `2`, WithLanguageExtensions())
	})

	t.Run("return LuaMultiReturn from catch", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function multi(...args: unknown[]) {
    return $multi(...args);
}

        function multiTest() {
            try {
                throw "error";
            } catch {
                return multi(1, 2);
            }
        }
        const [_, a] = multiTest();
        return a;`, `2`, WithLanguageExtensions())
	})

	t.Run("LuaMultiReturn applies after casting a function (#1404)", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `let swap: any = (a: number, b: number) => $multi(b, a);
        let [a, b] = (swap as (...args: any) => LuaMultiReturn<[number, number]>)(4, 3);
        return [a, b];`, `{3, 4}`, WithLanguageExtensions())
	})

	t.Run("LuaMultiReturn returns all values even when indexed with [0] #1411 Returns the correct value", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `
        declare function select<T>(this:void, index: '#', ...args: T[]): number;

        function foo(this: void): LuaMultiReturn<[number, number]> {
            return $multi(123, 456);
        }

        function bar(this: void): number {
            return foo()[0];
        }
    
export function __main() {return bar();}`, `123`, WithLanguageExtensions())
	})

	t.Run("LuaMultiReturn returns all values even when indexed with [0] #1411 Does not return multiple values", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `
        declare function select<T>(this:void, index: '#', ...args: T[]): number;

        function foo(this: void): LuaMultiReturn<[number, number]> {
            return $multi(123, 456);
        }

        function bar(this: void): number {
            return foo()[0];
        }
    
export function __main() {return select("#", bar());}`, `1`, WithLanguageExtensions())
	})
}
