// Code generated by scripts/migrate from extern/tstl/test/unit/jsx.spec.ts. DO NOT EDIT.
package tstltest

import "testing"

func TestEval_Jsx(t *testing.T) {
	t.Parallel()

	batchExpectDiagnostics(t, []diagTestCase{
		{"jsx invalid jsx config", "function", `import React, { Component } from "./react";
return <a/>`, []int32{100014}, nil},
	})

	t.Run("jsx element", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <foo></foo>}`, `{children = {}, type = "foo"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx self closing element", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <foo />}`, `{children = {}, type = "foo"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx element with dash name", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <with-dash />}`, `{children = {}, type = "with-dash"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx custom element", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {function Foo() {}
            return <Foo />}`, `{children = {}, type = "<< function >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx fragment", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <></>}`, `{children = {}, type = "<< class Fragment >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx fragment with children", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <><a foo="bar" /><b bar="foo" /></>}`, `{children = {{children = {}, props = {foo = "bar"}, type = "a"}, {children = {}, props = {bar = "foo"}, type = "b"}}, type = "<< class Fragment >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx esoteric component names", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {class _Foo extends Component {}
            return <_Foo />}`, `{children = {}, type = "<< class _Foo >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx esoteric component names", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {class $ extends Component {}
            return <$ />}`, `{children = {}, type = "<< class $ >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx esoteric component names", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {class é extends Component {}
            return <é />}`, `{children = {}, type = "<< class é >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx nested elements", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <foo><bar></bar></foo>}`, `{children = {{children = {}, type = "bar"}}, type = "foo"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx many nested elements", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <foo><a><b><bar></bar></b></a></foo>}`, `{children = {{children = {{children = {{children = {}, type = "bar"}}, type = "b"}}, type = "a"}}, type = "foo"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx interpolated children", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const x = 3
            return <foo>{x}</foo>}`, `{children = {3}, type = "foo"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx string prop", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <a foo="bar" />}`, `{children = {}, props = {foo = "bar"}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx value prop", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const x = 5
            return <a foo={x} />}`, `{children = {}, props = {foo = 5}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx quoted prop", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <a foo-bar={true} />}`, `{children = {}, props = {foo-bar = true}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx shorthand prop", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <a foo />}`, `{children = {}, props = {foo = true}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx spaces in jsxText", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <a>this
                <b/> is  some<a/>multiline
                text    <a/>   thing.
                <b/>
            </a>}`, `{children = {"this", {children = {}, type = "b"}, " is  some", {children = {}, type = "a"}, "multiline text    ", {children = {}, type = "a"}, "   thing.", {children = {}, type = "b"}}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx multiline string jsxText", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <a>
                foo  bar
                baz
            </a>}`, `{children = {"foo  bar baz"}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx access tag value", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const a = { b(){} };
            return <a.b c='d' />}`, `{children = {}, props = {c = "d"}, type = "<< function >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx access tag value", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const a = { b: { c: { d(){} } } };
            return <a.b.c.d c='d'/>}`, `{children = {}, props = {c = "d"}, type = "<< function >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx spread props", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const x = {c: "d", e: "f"}
            return <a {...x} />}`, `{children = {}, props = {c = "d", e = "f"}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx spread props", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const x = {c: "d", e: "no"}
            return <a a="b" {...x} e="f" />}`, `{children = {}, props = {a = "b", c = "d", e = "f"}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx comment children", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <a>
                {/* comment */}
                {/* another comment */}
            </a>}`, `{children = {}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx comment children", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <a>
                <b/>
                {/* comment */}
                <b/>
            </a>}`, `{children = {{children = {}, type = "b"}, {children = {}, type = "b"}}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx multiline string prop value", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <div value="This is a
               multi-line string."
      />}`, `{children = {}, props = {value = "This is a\n               multi-line string."}, type = "div"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx multiline string prop value", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <div value="
               This is a longer
               multi-line string.
                  "
      />}`, `{children = {}, props = {value = "\n               This is a longer\n               multi-line string.\n                  "}, type = "div"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx prop strings with entities", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <div value="a&gt;b" />}`, `{children = {}, props = {value = "a>b"}, type = "div"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx jsxText with entities", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {return <a> 9+10&lt;21 </a>}`, `{children = {" 9+10<21 "}, type = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx Spread children", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const children = [<a/>, <b/>]
            return <foo>{...children}</foo>}`, `{children = {{children = {}, type = "a"}, {children = {}, type = "b"}}, type = "foo"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx complex", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const x = 3
            const props = {one: "two", three: 4}
            return <div a="b">
                <a c="d" r={x}/>
                <b baz-bar {...props}> the {x}marks the spot </b>
                 a
                    {/** bar */}
                    and
                <> <a>2</a></>
                <b/>
            </div>}`, `{children = {{children = {}, props = {c = "d", r = 3}, type = "a"}, {children = {" the ", 3, "marks the spot "}, props = {baz-bar = true, one = "two", three = 4}, type = "b"}, "a", "and", {children = {" ", {children = {"2"}, type = "a"}}, type = "<< class Fragment >>"}, {children = {}, type = "b"}}, props = {a = "b"}, type = "div"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx custom JSX factory", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import { MyLib } from "./myJsx";
export function __main() {return <a><b>c</b></a>}`, `{children = {{children = {"c"}, myThing = true, type = "string"}}, myThing = true, type = "string"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithExtraFile("myJsx.ts", `export namespace MyLib {
        export function myCreate(
            type: any,
            props: any,
            ...children: any[]
        ) {
            return { type: typeof type, props, children, myThing: true };
        }

        export function MyFragment() {
        }
    }
    `), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2, "jsxFactory": "MyLib.myCreate"}))
	})

	t.Run("jsx custom JSX factory", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import { MyLib } from "./myJsx";const myCreate2 = MyLib.myCreate;
export function __main() {return <a><b>c</b></a>}`, `{children = {{children = {"c"}, myThing = true, type = "string"}}, myThing = true, type = "string"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithExtraFile("myJsx.ts", `export namespace MyLib {
        export function myCreate(
            type: any,
            props: any,
            ...children: any[]
        ) {
            return { type: typeof type, props, children, myThing: true };
        }

        export function MyFragment() {
        }
    }
    `), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2, "jsxFactory": "myCreate2"}))
	})

	t.Run("jsx custom JSX factory with noImplicitSelf", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `function createElement(tag: string | Function, props: { [key: string]: string | boolean }, ...children: any[]) {
                return { tag, children };
            }
export function __main() {return <a><b>c</b></a>}`, `{children = {{children = {"c"}, tag = "b"}}, tag = "a"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2, "jsxFactory": "createElement", "noImplicitSelf": true}))
	})

	t.Run("jsx custom fragment factory", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import { MyLib } from "./myJsx";
export function __main() {return <><b>c</b></>}`, `{children = {{children = {"c"}, myThing = true, type = "string"}}, myThing = true, type = "function"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithExtraFile("myJsx.ts", `export namespace MyLib {
        export function myCreate(
            type: any,
            props: any,
            ...children: any[]
        ) {
            return { type: typeof type, props, children, myThing: true };
        }

        export function MyFragment() {
        }
    }
    `), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2, "jsxFactory": "MyLib.myCreate", "jsxFragmentFactory": "MyLib.MyFragment"}))
	})

	t.Run("jsx custom fragment factory", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import { MyLib } from "./myJsx";function MyFragment2(){};
export function __main() {return <><b>c</b></>}`, `{children = {{children = {"c"}, myThing = true, type = "string"}}, myThing = true, type = "function"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithExtraFile("myJsx.ts", `export namespace MyLib {
        export function myCreate(
            type: any,
            props: any,
            ...children: any[]
        ) {
            return { type: typeof type, props, children, myThing: true };
        }

        export function MyFragment() {
        }
    }
    `), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2, "jsxFactory": "MyLib.myCreate", "jsxFragmentFactory": "MyFragment2"}))
	})

	t.Run("jsx custom JSX pragma", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `/** @jsx MyLib.myCreate */
import { MyLib } from "./myJsx";
export function __main() {return <a><b>c</b></a>}`, `{children = {{children = {"c"}, myThing = true, type = "string"}}, myThing = true, type = "string"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithExtraFile("myJsx.ts", `export namespace MyLib {
        export function myCreate(
            type: any,
            props: any,
            ...children: any[]
        ) {
            return { type: typeof type, props, children, myThing: true };
        }

        export function MyFragment() {
        }
    }
    `), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx custom JSX pragma", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `/** @jsx myCreate2 */import { MyLib } from "./myJsx";const myCreate2 = MyLib.myCreate;
export function __main() {return <a><b>c</b></a>}`, `{children = {{children = {"c"}, myThing = true, type = "string"}}, myThing = true, type = "string"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithExtraFile("myJsx.ts", `export namespace MyLib {
        export function myCreate(
            type: any,
            props: any,
            ...children: any[]
        ) {
            return { type: typeof type, props, children, myThing: true };
        }

        export function MyFragment() {
        }
    }
    `), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx custom fragment pragma", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `/** @jsx MyLib.myCreate */
/** @jsxFrag MyLib.MyFragment */
import { MyLib } from "./myJsx";
export function __main() {return <><b>c</b></>}`, `{children = {{children = {"c"}, myThing = true, type = "string"}}, myThing = true, type = "function"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithExtraFile("myJsx.ts", `export namespace MyLib {
        export function myCreate(
            type: any,
            props: any,
            ...children: any[]
        ) {
            return { type: typeof type, props, children, myThing: true };
        }

        export function MyFragment() {
        }
    }
    `), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})

	t.Run("jsx custom fragment pragma", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `/** @jsx MyLib.myCreate */
/** @jsxFrag MyFragment2 */
import { MyLib } from "./myJsx";function MyFragment2(){};
export function __main() {return <><b>c</b></>}`, `{children = {{children = {"c"}, myThing = true, type = "string"}}, myThing = true, type = "function"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithExtraFile("myJsx.ts", `export namespace MyLib {
        export function myCreate(
            type: any,
            props: any,
            ...children: any[]
        ) {
            return { type: typeof type, props, children, myThing: true };
        }

        export function MyFragment() {
        }
    }
    `), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2, "jsxFactory": "MyLib.myCreate", "jsxFragmentFactory": "MyFragment"}))
	})

	t.Run("jsx forward declare components", func(t *testing.T) {
		t.Parallel()
		expectFunction(t, `import React, { Component } from "./react";
export function __main() {const foo = <Foo />

            function Foo(){}

            return foo}`, `{children = {}, type = "<< function >>"}`, WithExtraFile("react.ts", "\n    export class Component {\n        static isClass = true\n    }\n\n    namespace React {\n        const isLua = typeof Component === \"object\"\n\n        export function createElement(\n            type: any,\n            props?: any,\n            ...children: any[]\n        ) {\n            let typeStr: string\n            if (isLua) {\n                typeStr =\n                    typeof type === \"function\" ? \"<< function >>\" :\n                    typeof type === \"object\" ? `<< class ${type.name} >>` :\n                    type\n            } else {\n                typeStr = typeof type === \"function\" ? (type.isClass\n                                                        ? `<< class ${type.name} >>`\n                                                        : \"<< function >>\")\n                                                     : type\n            }\n\n            return {\n                type: typeStr,\n                props,\n                children\n            };\n        }\n\n        export class Fragment extends Component {\n        }\n    }\n    export default React;\n"), WithExtraFile("jsx.d.ts", `declare namespace JSX {
    interface IntrinsicElements {
        a: any
        b: any
        foo: any
        bar: any
        div: any
        "with-dash": any
    }
}
`), WithMainFileName("main.tsx"), WithOptions(map[string]any{"jsx": 2}))
	})
}
