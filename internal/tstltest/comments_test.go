// Code generated by scripts/migrate from extern/tstl/test/unit/comments.spec.ts. DO NOT EDIT.
package tstltest

import "testing"

func TestEval_Comments(t *testing.T) {
	t.Parallel()

	batchExpectDiagnostics(t, []diagTestCase{
		{"Single-line JSDoc is copied on a function", "module", `/** This is a function comment. */
        function foo() {}`, nil, []string{`--- This is a function comment.
function foo(self)
end`}},
		{"Multi-line JSDoc with one block is copied on a function", "module", `/**
         * This is a function comment.
         * It has more than one line.
         */
        function foo() {}`, nil, []string{`--- This is a function comment.
-- It has more than one line.
function foo(self)
end`}},
		{"Multi-line JSDoc with two blocks is copied on a function", "module", `/**
         * This is a function comment.
         * It has more than one line.
         *
         * It also has more than one block.
         */
        function foo() {}`, nil, []string{`--- This is a function comment.
-- It has more than one line.
-- 
-- It also has more than one block.
function foo(self)
end`}},
		{"JSDoc is copied on a function with tags", "module", `/**
         * This is a function comment.
         * It has multiple lines.
         *
         * @param arg1 This is the first argument.
         * @param arg2 This is the second argument.
         * @returns A very powerful string.
         */
        function foo(arg1: boolean, arg2: number): string {
            return "bar";
        }`, nil, []string{`--- This is a function comment.
-- It has multiple lines.
-- 
-- @param arg1 This is the first argument.
-- @param arg2 This is the second argument.
-- @returns A very powerful string.
function foo(self, arg1, arg2)
    return "bar"
end`}},
		{"JSDoc is copied on a variable", "module", `/** This is a variable comment. */
        const foo = 123;`, nil, []string{`--- This is a variable comment.
foo = 123`}},
	})

	batchExpectCodegen(t, []codegenTestCase{
		{"Single-line JSDoc is copied on a function", "module", `/** This is a function comment. */
        function foo() {}`, nil, nil, nil, nil},
		{"Multi-line JSDoc with one block is copied on a function", "module", `/**
         * This is a function comment.
         * It has more than one line.
         */
        function foo() {}`, nil, nil, nil, nil},
		{"Multi-line JSDoc with two blocks is copied on a function", "module", `/**
         * This is a function comment.
         * It has more than one line.
         *
         * It also has more than one block.
         */
        function foo() {}`, nil, nil, nil, nil},
		{"JSDoc is copied on a function with tags", "module", `/**
         * This is a function comment.
         * It has multiple lines.
         *
         * @param arg1 This is the first argument.
         * @param arg2 This is the second argument.
         * @returns A very powerful string.
         */
        function foo(arg1: boolean, arg2: number): string {
            return "bar";
        }`, nil, nil, nil, nil},
		{"JSDoc is copied on a variable", "module", `/** This is a variable comment. */
        const foo = 123;`, nil, nil, nil, nil},
	})
}
